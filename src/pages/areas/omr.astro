---
import AreaLayout from "~/layouts/AreaLayout.astro";
import CtaStrip from "~/components/CtaStrip.astro";
import { areaByKey, type AreaData } from "~/content/areas";

const a: AreaData = areaByKey["omr"];
---

<AreaLayout areaData={a}>
  <CtaStrip
    title={`Need M-Sand or aggregate in ${a.name}?`}
    body="Call us directly or WhatsApp your BOQ. We quote per-ton, per-unit, and delivery in under a minute."
  />
</AreaLayout>
